const defaultState = {

}

const reducer = (state = defaultState, action) => {
  switch (action.type) {
    case "expression":

      break;
    default:
      return state;
  }
}

export default reducer;
